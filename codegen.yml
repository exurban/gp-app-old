overwrite: true
schema: "http://localhost:4000/api"
documents: "./src/graphql/**/*.graphql"
generates:
  ./src/typed-document-nodes.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typed-document-node"
    # config:
    #   scalars:
    #     ID: number
  ./graphql.schema.json:
    plugins:
      - "introspection"

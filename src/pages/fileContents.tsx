import * as file from "../graphql/photos.graphql";

// * Can use this on a blank page to show the queries and mutations contained in a *.graphql file
const IndexPage: React.FC = () => {
  console.log(file);

  return <div>File</div>;
};

export default IndexPage;

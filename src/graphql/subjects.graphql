query allPhotosOfSubject($input: AllPhotosOfSubjectInput!) {
  allPhotosOfSubject(input: $input) {
    subjectInfo {
      id
      name
      description
      coverImage {
        ...ImageInfo
      }
      createdAt
      updatedAt
    }
    photos {
      ...PhotoInfo
    }
    startCursor
    endCursor
    total
  }
}

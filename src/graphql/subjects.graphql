query subjectWithName($input: SubjectInput!) {
  subjectWithName(input: $input) {
    id
    name
    createdAt
    updatedAt
    photosOfSubject {
      photo {
        ...PhotoInfo
      }
    }
  }
}

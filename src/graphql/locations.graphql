query allPhotosAtLocation($input: AllPhotosAtLocationInput!) {
  allPhotosAtLocation(input: $input) {
    locationInfo {
      id
      name
      tag
      description
      coverImage {
        ...ImageInfo
      }
    }
    items {
      ...PhotoInfo
    }
    startCursor
    endCursor
    total
  }
}

query allPhotosAtLocation($input: AllPhotosAtLocationInput!) {
  allPhotosAtLocation(input: $input) {
    locationInfo {
      id
      name
      tag
      description
      coverImage {
        ...ImageInfo
      }
    }
    photos {
      ...PhotoInfo
    }
    startCursor
    endCursor
    total
  }
}
